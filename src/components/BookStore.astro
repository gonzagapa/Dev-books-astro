---
import { SCORE_API_ENDPOINT } from "astro:env/server";

const res = await fetch(SCORE_API_ENDPOINT);
const value = await res.text();
---

<span class="text-sm block mt-5" id="score"
  >⭐ Puntuacion: {value.trim()}/5</span
>
